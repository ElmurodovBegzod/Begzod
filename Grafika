import turtle
import time
turtle.bgcolor('blue')
turtle.pensize(2)
turtle.speed(0)
for i in range(6):
    for colours in ('red', 'black', 'yellow', 'green', 'white', 'magenta'):
        turtle.color(colours)
        turtle.circle(120)
        turtle.left(10)
    turtle.hideturtle()
time.sleep(10)
